//Modules -info about current module (file)

const john = 'john'
const peter = 'peter'
const mujib = 'mujib'

const sayHi = (name) => {
    console.log(`Hello there ${name}`)
}

sayHi('susan')
sayHi(mujib)